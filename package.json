{
  "name": "url-simplifier-app",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix ./apps/backend && npm run build --prefix ./apps/backend && npm install --prefix ./apps/frontend"
  }
}